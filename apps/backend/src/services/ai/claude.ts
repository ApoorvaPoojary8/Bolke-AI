/**
 * claude.ts — LEGACY / UNUSED
 *
 * Claude is no longer the primary AI provider.
 * Groq Llama 3.3 70B is now primary (see groq.ts + router.ts).
 *
 * This file is kept for reference but nothing imports it.
 * The @anthropic-ai/sdk package has been removed from package.json.
 */

export {};
